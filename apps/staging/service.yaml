apiVersion: v1
kind: Service
metadata:
  name: myapp-staging
  namespace: staging
spec:
  selector:
    app: myapp-staging
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: NodePort